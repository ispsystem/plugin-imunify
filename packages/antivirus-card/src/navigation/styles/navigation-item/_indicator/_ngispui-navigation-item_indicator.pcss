.ngispui-navigation-item_indicator:not(.ngispui-navigation-item_active) {
  position: relative;

  &::after {
    position: absolute;
    top: calc(100% + 10px);
    left: calc(50% - 2.5px);

    display: block;

    width: 5px;
    height: 5px;

    content: '';

    border-radius: 5px;
    background-color: var(--ngispui-navigation-item__indicator-color, var(--isp-color__badge, #e44592));
  }
}
